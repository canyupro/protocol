# 命名规范

> 编码规约根目录：`doc/protocol/coding-standards/`
> 本文件定义项目应使用的命名字段框架。具体命名风格由项目方按 UI 框架（Tailwind / Ant Design / 自研）填入。
>
> **引用关系**：本文档是规范定义；项目实际使用 + 异常记在 `understanding/&lt;项目名&gt;.md` 下，**通过字段引用**指向本文档对应小节（不要复制粘贴）。

---

## 0. 一段话人话摘要

> 项目整体：[项目方填入。例如「类名 `T` 前缀 + PascalCase；表名 `t_` 前缀 + snake_case；Controller 路径前缀按端口语义」]。
> 例外：[N] 类（见 §5 表格「状态=已验证例外」行）。

---

## 1. 目录与模块

### 1.1 一段话（不超过 3 行）

> [项目方填入。例如「根目录 N 个模块，业务模块名以 X 开头，公共核心为 Y」]。

### 1.2 表格（机读友好）

| 样本路径 | 观察到的约定 | 状态 | 证据 |
|---|---|---|---|
| `[路径样本]` | `[约定描述]` | `[已验证 / 已验证例外 / 假设 / 待确认]` | `[文件:行号 或探索方法]` |

---

## 2. 类型、接口、事件与函数

### 2.1 一段话

> [项目方填入。例如「表实体 `T` 前缀；Mapper `XxxMapper`；Service `XxxService + XxxServiceImpl`；Controller 按端语义命名」]。

### 2.2 表格

| 样本 | 观察到的约定 | 状态 | 证据 |
|---|---|---|---|
| `[类名/接口名]` | `[约定描述]` | `[状态]` | `[文件:行号]` |

---

## 3. 测试命名与位置

### 3.1 一段话

> [项目方填入。例如「项目测试覆盖率现状 / 框架 / 命名风格」]。

### 3.2 表格

| 样本 | 观察到的约定 | 状态 | 证据 |
|---|---|---|---|

---

## 4. 本任务新增内容的建议落点

- 路径：`[项目方填入，落哪个目录/包]`
- 命名：`[项目方填入，新内容应遵循的命名规则]`
- 原因：`[为什么这么选]`
- 不选择相邻位置的原因：`[为什么不放隔壁]`

---

## 5. 历史例外与待确认项

### 5.1 历史例外（已确认不改）

- [项目方填入 N1：路径/类名/函数名 — 问题描述 — 决策：保留/重命名/隔离 — 证据：文件:行号]

### 5.2 待确认项（必须挂超时）

- [ ] [项目方填入路径/类名 — 问题描述 — 提出日期：YYYY-MM-DD — 负责人：待指派]
- [ ] ...

> 协议区 task.md 与 `00-基础调查问卷.md` 的 `[待确认]` 项应与本节双向引用，避免悬挂。
