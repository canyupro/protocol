# 当前任务与权限边界

## 任务生命周期

> 由守卫（`protocol_guard.py`）读取 `当前状态` 字段。任务撤回时已签发的批准自动失效，需重签。
> 推荐：把「整体撤回」「任务已废止」等语义事件记录在本段，不要写进 `## 任务` 段当 prose，
> 否则 agent 容易把"事实描述"误判为"任务当前状态"。

- 当前状态：草稿 / 进行中 / 已撤回（YYYY-MM-DD）/ 已完成（YYYY-MM-DD）
- 开始时间（ISO8601，由 agent 在 `/protocol:begin` 时自动写入，禁止手填覆盖）：
- 发起方：human / agent-auto / scheduled
- 当前会话 ID：
- 撤回时间（ISO8601，撤回时填写）：
- 撤回原因：
- 撤回人：
- 影响：本任务已签发的批准自动失效；guard 拒绝所有 Edit/Write 直到下次 begin。

## 任务

- 目标：
- 交付物：
- 明确不做什么：
- 验收标准：
- 确认人：

## 权限边界

- 可读：
- 可写：
- 可执行：
- 可测试：
- 可访问环境/数据：
- 可发布：

## 人类可兜底边界

- 当前负责人：
- 可兜底范围：
- 需询问维护者的条件：
- 需升级项目负责人/领导的条件：

## 事实状态

- 已验证事实：
- 暂定假设：
- 待确认项：
