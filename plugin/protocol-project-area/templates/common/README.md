# Protocol 项目区

> 这份文档是项目的**人读入口**，按「渐进、由粗到细」组织：先看懂系统是什么，再看关键协作，最后才下钻到细节。第一次读项目从这里开始。

---

## 一页看懂：这个项目是什么

<!-- 初始化时按项目事实替换为本项目的一句话介绍（人类语言，不用文件计数、不抛深层路径）。
示例：这是一个〔行业/场景〕的〔类型〕平台：〔谁〕在〔哪个入口〕做〔什么事〕，
由〔N〕个服务组成，围绕同一套〔数据/中间件〕运行。 -->

> 一句话介绍：`[初始化时填写]`

---

## 关键模块与协作

| 模块/服务 | 职责（一句话） | 端口 | 备注 |
|---|---|---|---|
| `[模块A]` | `[一句话职责]` | `[端口]` | `[谁在用/边界]` |
| `[模块B]` | `[一句话职责]` | `[端口]` | `[谁在用/边界]` |

<!-- 初始化时按实际模块补充；这里只放「职责一句话 + 端口 + 边界」，协作细节放到下方小节。 -->

**协作要点**（初始化时按实际补充）：
- 依赖方向：`[哪些服务依赖公共核心/谁被谁调用]`
- 数据与中间件：`[是否共用数据库/缓存/消息队列，改一端影响何端]`
- 边界提醒：`[是否微服务隔离、有无仓库外依赖系统]`

---

## 需要细节时，再下钻

> 下面这些是给 **AI / 深入查阅** 的细节底稿（含包路径、文件计数、命名异常等）。**人读请先看上面总览，需要具体细节时再点开**。

| 想了解 | 去这里 |
|---|---|
| 基础调查问卷（模式/权限/等级/负责人） | `00-基础调查问卷.md` |
| 整体架构图（可编辑 drawio） | `understanding/architecture.drawio` |
| 数据流动图 | `understanding/data-flow.drawio` |
| 命名规范与历史例外 | `understanding/naming.md` |
| 各模块详细说明 | `modules/`（见 `modules/README.md`） |
| 影响面 / 任务记录 / 冷启动回顾 | `impact/` `tasks/` `reviews/` |
| 人类批准与豁免 | 仅在需要解锁代码写入时，由人类在 ZCode 工具路径之外按插件模板签发到 `approvals/` |

> 项目区机器状态（当前任务、文件映射、守卫状态、验证报告）位于 `.agents/protocol/`，主要是给 AI 与协议流程用，人读无需关注。

---

## 维护约定

- 本 README 只保留「渐进总览」，详细事实只在其唯一归属文档中维护（单一事实来源）。
- 细节底稿（`understanding/`、`modules/`）更新时，如影响总览所述职责/边界/协作，须同步回本 README，避免两份视图矛盾。
