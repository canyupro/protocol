---
description: 初始化 Protocol 项目区与基础调查问卷
argument-hint: "[项目说明]"
skills: project-area-init
---

使用 `project-area-init` skill 初始化当前工作区的 Protocol 项目区。

初始化只创建无 guard、无 approvals 的项目区骨架。之后必须读取问卷、展示既有 `AGENTS.md` 的拟议短指针并取得用户确认；守卫只能在后续确认阶段显式建立。

$ARGUMENTS
